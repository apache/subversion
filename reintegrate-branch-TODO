Task list for the "reintegrate" branch.

Merge-back-to-trunk blockers
============================

* Correct the "is there any real mergeinfo?" check. (glasser)

* Propagate the new include_decendents boolean to the DAV RA layers. (kfogel)

* Port FSFS mergeinfo index changes to BDB backend. (glasser/cmpilato?)

* Add black box tests for 'merge --reintegrate'. (kfogel)

* Deal with renamed merge target, when the mergeinfo is up-to-date, but
  under an unexpected name. (dlr)


Remaining work (which can be done post-merge)
=============================================

* Fix UI constraints for 'merge --reintegrate', which is "incompatible"
  with --ignore-ancestry and --record-only (dlr)

* Add C test for svn_mergeinfo_intersect(). (dlr)

* Examine "TODO(reint)", including (but not limited to):

  * Error messages (kfogel)

  * "Is this the same line?" direct ancestor check (glasser)

  * authz check (glasser)

* Scrutinize pool usage in FSFS changes. (glasser)
